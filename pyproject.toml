[project]
name = "endnote_to_zotero"
version = "0.0.1"
description = "Simple commandline tool to convert EndNote Word documents to Zotero."

[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
